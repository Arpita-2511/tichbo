package com.eventtick.catalog;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class CatalogServiceApplicationTests {

    @Test
    void contextLoads() {
        // Verifies the Spring application context starts successfully,
        // including datasource/JPA auto-configuration against the
        // in-memory test database (see src/test/resources/application.yml).
    }
}
