<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.4</version>
        <relativePath/>
    </parent>

    <groupId>com.eventtick</groupId>
    <artifactId>gateway-service</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <name>gateway-service</name>
    <description>
        Eventtick API Gateway — single entry point for client requests.
        Skeleton only: no routes, filters, or security configured yet.
    </description>

    <properties>
        <java.version>17</java.version>
        <spring-cloud.version>2023.0.3</spring-cloud.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Spring Cloud Gateway (reactive): routing (Phase 7.1) and CORS
             (Phase 7.2). -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-gateway</artifactId>
        </dependency>

        <!-- Phase 7.3: JWT validation at the gateway. This is Spring
             Security's reactive (WebFlux) resource-server support plus the
             Nimbus JOSE library it uses to verify signatures — the
             gateway is reactive, so the servlet-based approach used in
             user-service does not apply here. It is the only new
             dependency: no JJWT (Nimbus already does the verification) and
             no OAuth2 *client*/authorization-server pieces. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>

        <!-- Phase 11: static Redis-backed rate limiting. Brings the reactive
             Lettuce client and auto-configures ReactiveStringRedisTemplate,
             which is what turns on Spring Cloud Gateway's own
             GatewayRedisAutoConfiguration (RedisRateLimiter bean) — see
             config/GatewayRateLimitConfig and filter/RateLimitingGlobalFilter.
             It is the only new dependency. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis-reactive</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Test-only: a real redis-server process (no Docker/WSL required in
             this environment), so rate-limiter tests exercise the actual
             Redis-backed Lua token bucket instead of a hand-rolled fake. See
             docs/architecture.md, "Redis testing (Phase 11)", for why. -->
        <dependency>
            <groupId>com.github.codemonstur</groupId>
            <artifactId>embedded-redis</artifactId>
            <version>1.4.3</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
