/**
 * Spring configuration for the API Gateway — CORS, route definitions,
 * security filter chain, and rate-limit wiring will live here.
 *
 * <p>Not implemented yet; this package establishes where that
 * configuration will go once the Gateway routing phase begins.
 */
package com.eventtick.gateway.config;
