/**
 * JPA entities mapping to the {@code users} and {@code plans} tables.
 *
 * <p>Not implemented yet. When added, these must mirror the hand-authored
 * schema in {@code database/migrations/} exactly — {@code ddl-auto} is set
 * to {@code none} (see {@code application.yml}), so Hibernate never
 * generates or alters schema; the SQL migrations remain the single source
 * of truth.
 */
package com.eventtick.user.entity;
